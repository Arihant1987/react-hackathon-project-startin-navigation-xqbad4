import React from "react";
import Landing from './landing';

const Insights = () => {
return( 
  <div class="basic-layout">
    <Landing />
    Insights
  </div>
);
};

export default Insights;